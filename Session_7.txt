7.1
CREATE TABLE Restaurant (
    id INT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    location VARCHAR(100) NOT NULL,
    cuisine VARCHAR(100)
);

7.2
CREATE TABLE FoodOrder (
    order_id INT PRIMARY KEY,
    restaurant_id INT,
    user_id INT,
    order_total DECIMAL(10,2),
    FOREIGN KEY (restaurant_id) REFERENCES Restaurant(id)
);

7.3
CREATE TABLE SpotifyUser (
    user_id INT PRIMARY KEY,
    username VARCHAR(50) UNIQUE,
    email VARCHAR(100) NOT NULL,
    subscription_type VARCHAR(50)
);

INSERT INTO SpotifyUser (user_id, username, email, subscription_type)
VALUES
(1, 'musiclover', 'musiclover@gmail.com', 'Premium'),
(2, 'rahul123', 'rahul@gmail.com', 'Free'),
(3, 'riya_music', 'riya@gmail.com', 'Premium'),
(4, 'arjunbeats', 'arjun@gmail.com', 'Premium'),
(5, 'neha_songs', 'neha@gmail.com', 'Free'),
(6, 'aman_playlist', 'aman@gmail.com', 'Premium');

7.4
CREATE VIEW TopSpendersView AS
SELECT SpotifyUser.username, FoodOrder.order_total FROM SpotifyUser
JOIN FoodOrder
ON SpotifyUser.user_id = FoodOrder.user_id
WHERE FoodOrder.order_total > 1000;

7.5
SELECT username, order_total FROM TopSpendersView
WHERE order_total > 2000
ORDER BY order_total DESC;