3.1
CREATE TABLE Restaurants (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    cuisine VARCHAR(100),
    rating DECIMAL(2,1),
    city VARCHAR(100)
);

INSERT INTO Restaurants (name, cuisine, rating, city)
VALUES
('Spice Villa', 'Indian', 4.5, 'Ahmedabad'),
('Pizza House', 'Italian', 4.2, 'Ahmedabad'),
('Sushi World', 'Japanese', 4.8, 'Mumbai'),
('Taco Fiesta', 'Mexican', 3.2, 'Delhi'),
('Burger Point', 'American', 3.8, 'Jaipur');

3.2
UPDATE Restaurants
SET rating = 4.7
WHERE name = 'Spice Villa';

3.3
DELETE FROM Restaurants
WHERE rating < 3.5;

3.4
SELECT * FROM Restaurants
WHERE city = 'Ahmedabad' AND rating > 4.0
LIMIT 2;